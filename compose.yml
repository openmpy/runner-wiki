services:
  springboot:
    container_name: server
    image: ${DOCKERHUB_USERNAME}/wiki-server:latest
    restart: unless-stopped
    ports:
      - 8080:8080
    networks:
      - wiki-network

networks:
  wiki-network:
    driver: bridge
